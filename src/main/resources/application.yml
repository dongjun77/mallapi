spring:
  application:
    name: mallapi

  datasource:
#    url: jdbc:mysql://localhost:3306/malldb
#    url : jdbc:mysql://instagram-db.cv2i2iyuwful.ap-northeast-2.rds.amazonaws.com/malldb
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    username: malldbuser
#    password: malldbuser
    url: jdbc:postgresql://localhost:5432/malldb
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  servlet:
    multipart:
      max-request-size: 30MB
      max-file-size: 10MB

logging:
  level:
    com.zaxxer: info
    org.springframework.security.web: trace
#    org.springframework.data.mongodb.core: DEBUG
#    org.mongodb.driver: DEBUG

# local file
org:
  zerock:
    upload:
      todo:
        path: upload/todo_files
      product:
        path: upload/product_files
      mongotodo:
        path: upload/mongotodo_files
